# OpenAI API key (required for chat). Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: model (default gpt-4o-mini). Use gpt-4o for higher quality.
# CHAT_MODEL=gpt-4o

# Optional: port (default 3001). Vite proxies /api to this port in dev.
# PORT=3001
